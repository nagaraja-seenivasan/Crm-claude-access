{
  "name": "crm-rule-builder",
  "version": "1.0.0",
  "description": "CRM drag-and-drop business rule builder",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "start:backend": "npm run build --prefix backend && node -r source-map-support/register backend/dist/index.js",
    "start:frontend": "npm start --prefix frontend",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
